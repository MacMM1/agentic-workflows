# Tools

This folder should contain deterministic, single-purpose execution scripts.

Keep each tool:

- small
- testable
- idempotent where possible
- independent of UI state
- focused on one job only
