# Workflows

This folder should contain one markdown SOP per repeatable process.

Recommended structure:

- objective
- required inputs
- execution steps
- expected outputs
- error handling
- verification

Start with a simple workflow and keep it explicit enough that a new agent can execute it without guesswork.
